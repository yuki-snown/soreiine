version: '3'

services:
  eval:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - "./api:/api"
    tty: true
    environment:
      TZ: Asia/Tokyo
    command: flask run --host 0.0.0.0 --port 5000